% from bottle import template
% for video in mylist:
  <a class="video-tag" href="javascript:void(0)" video="{{ video["watch_id"][2:] }}" title_data="{{! video["title"]}}">{{! video["title"] }}</a>
% end
